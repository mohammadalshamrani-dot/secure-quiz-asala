services:
  - type: web
    name: secure-quiz-asala
    env: node
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: JWT_SECRET
        value: asala-quiz-2025-secret