services:
  - type: web
    name: secure-quiz-asala
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: ADMIN_KEY
        value: asala2025
